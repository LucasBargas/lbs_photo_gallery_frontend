import { useRouter } from 'next/router';
import React, { useEffect, useState } from 'react';

const User = () => {
  const { query } = useRouter();

  return <div>User</div>;
};

export default User;
